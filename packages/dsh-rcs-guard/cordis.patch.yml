# 安全层。mode 可选 dev（默认）或 training。
# training 只有两处不同：L2 的确认文案更重、LG 代码生成要过闸门。
# 两种模式都不会拒绝任何调用 —— 曾经的 field（赛场只读）已删除，见 rcs-core/danger.ts。
- insert:
    - id: rcs-guard
      name: dsh-rcs-guard
      config:
        mode: dev
        extraL2: []
