# dsh4rcs 工作区说明

厦门大学 RCS 机器人队的 dsh 插件套件。5 个插件、20 个工具，服务 ROBOCON 2027「女娲补天」赛季的电控方向。

## 先用工具，别凭记忆

规则条款、队内文档、工程检查结果**都有对应工具**，它们读的是真实数据：

| 想知道 | 用 |
|---|---|
| 赛季/主题/机器人限制/倒计时 | `rcs_team_context` |
| 规则怎么写的 | `rcs_rule_lookup`（返回条款号 + 版本 + 原文） |
| 两版规则改了什么 | `rcs_rule_diff` |
| 队内文档怎么说 | `rcs_kb_search`（离线，读本地镜像） |
| 工程有没有违反分层/嵌入式规范 | `rcs_lint_layer` / `rcs_lint_embedded` |
| 舵轮回环、底盘解算有没有问题 | `rcs_angle_loop_check` / `rcs_kinematics_check` |
| RDLC 报文是什么 | `rcs_rdlc_decode` |
| 本机工具链齐不齐 | `rcs_toolchain_status` |

**规则解读错的代价是整套方案返工**，所以工具只返回原文与条款号，判断留给人。回答规则问题时务必带上条款号和版本号——脱离版本的条款号是危险的。

## 危险分级（涉及人身安全）

- **L0 只读** —— 放行
- **L1 本机写**（`rcs_fw_build`、`rcs_support_test`、`rcs_kb_sync`）—— 开发放行，赛场拒绝
- **L2 物理动作**（`rcs_fw_flash`、电机使能、气路动作、总线下发）—— 开发需人工确认，**赛场一律拒绝**

规则 12.2 强制要求红色急停按钮。**软件停止永远不能替代硬件急停、驱动使能线和限位保护**——任何涉及硬件的建议都不要绕过这一条。

## 这个仓库反复付过学费的几条

- **误报比漏报更伤。** 天天喊狼来了的检查没人看。分层检查曾一次喷 87 条、嵌入式检查曾喷 253 条，收敛到 14 和 7 才有人用。
- **假绿比红更危险。** 曾用假 token 探飞书权限，两轮都报「已开通」，拿真文档一试才发现根本没批。检查项宁可报「未验证」，也不要给一个没验过的勾。
- **静默丢数据最危险。** 规则提取曾漏掉整条 11.16，167 条跑通了但少一条没人看得出来。
- **失败却说不出原因是最糟的输出。** 它逼人去手工翻日志，那工具就白做了。

## 已知问题（别重复诊断）

- `RCS_code/demo/RCS/user/host_link.cpp:89` 有一处 narrowing 编译错误。修法在同仓库的 `demo_function_dispatch` 里已存在（加 `(uint8_t)` cast）。**尚未修**。
- `kin_chassis.cpp` 有三处缺陷：弧度/角度单位错配导致 `find_nearest` 退化为空操作、`rcs_omni4::inv_kin` 返回未初始化栈内存、`||`/`&&` 优先级混用。详见 `FEATURES.md` 附录。
- 路线图里的 CAN 缓冲区崩溃（git log `fa9056c`）优先级高于本插件的任何事项。

## 环境事实

- `RCS_code` **不是 git 仓库**，改动前先备份。
- Keil 自带并授权的是 **ArmCompiler V6.22**；单独安装的 V6.24 没有授权，工程选错会报成 license 错误。
- PC 单元测试的 gtest 静态库是 **Linux ELF**，只能在 WSL 内构建。
- dsh 全套锁在 **0.1.0-rc.6**（服务端与 profile 必须一致，只动一边会让网页端永远停在 loading）。
